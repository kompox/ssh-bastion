---
id: task-20260103d-sshd-container
title: SSHD container (local compose)
status: done
updated: 2026-01-03T09:46:52Z
---
# Task: SSHD container (local compose)

## Goal

Add an `sshd` container to the local runtime topology, using the existing image and shared `/data` volume, as the next step toward the target multi-container Pod model.

## Scope

### In

- Implement a dedicated `sshd` container in local docker-compose.
- Use the same published image (`ghcr.io/kompox/ssh-bastion`) and shared `/data` volume.
- Define how `sshd` reads authorized keys from `/data` (compatible with the existing “generate files under /data” approach).

### Out

- Production hardening (securityContext/capabilities, read-only rootfs, etc.).
- Kubernetes manifests/Helm.
- SSH forwarding policy and restrictions.

## Spec (summary)

- Roadmap item: “Container development and local testing” in [design-roadmap].
- Container topology and conventions: [design-containers].

## Plan & Checklist

- [x] Decide file locations under `/data` for `authorized_keys` and host keys
  - Authorized keys: `/data/authorized_keys/jump` (already generated by the web app)
  - Host keys: `/data/ssh/ssh_host_*` (persist across restarts)
- [x] Add compose service for `sshd` (from the same image) with correct mounts
- [x] Ensure `sshd` can start reliably (host keys present/generated)
- [x] Ensure the `authorized_keys` path wiring matches the future generator behavior
- [x] Document how to run/test locally (minimal)

### Minimal local test procedure

1. Start compose:

```bash
docker compose up -d --build
```

2. Add your public key via the web UI:

- Open: `http://localhost:8080/`
- Add your public key (the corresponding private key stays on your machine)

3. SSH into the bastion:

```bash
ssh -4 -p 2222 -i ~/.ssh/<your_key> jump@127.0.0.1
```

## Progress

- 2026-01-03T08:17:07Z
  - Create task document and add roadmap entry

- 2026-01-03T08:35:30Z
  - Add `sshd` entrypoint script and docker-compose service (port 2222->22)
  - Define `/data` paths for `authorized_keys` and host keys

- 2026-01-03T09:46:52Z
  - Move the container entrypoint to `docker/ssh-entrypoint.sh` and update compose/Dockerfile references
  - Add/expand `.dockerignore` to avoid rebuilds from unrelated changes (e.g. `_tmp/`, `e2e/`)
  - Verify `make e2e` passes with the updated layout

## References

- [design-roadmap] - Development Roadmap
- [design-containers] - Containers (image + runtime topology)

[design-roadmap]: ../design/design-roadmap.md
[design-containers]: ../design/design-containers.md
